import React from 'react';

export interface item {
    id: number;
    title: string;
    title_long: string;
    tag: string;
    thumb: string;
    image: string;
    slug: string;
    questions: string;
}